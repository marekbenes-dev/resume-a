import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.marekbenes.resume',
  appName: 'resume-a',
  webDir: 'dist/resume-a/browser'
};

export default config;
